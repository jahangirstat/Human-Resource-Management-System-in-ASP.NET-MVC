@model IEnumerable<MVCFinalProject.Models.Designation>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DesName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DesName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DesId }) |
            @Html.ActionLink("Details", "Details", new { id=item.DesId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DesId })
        </td>
    </tr>
}

</table>
