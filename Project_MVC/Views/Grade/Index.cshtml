@model IEnumerable<MVCFinalProject.Models.Grade>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Grade_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Grade_Salary)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Grade_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Grade_Salary)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.GID }) |
            @Html.ActionLink("Details", "Details", new { id=item.GID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.GID })
        </td>
    </tr>
}

</table>
